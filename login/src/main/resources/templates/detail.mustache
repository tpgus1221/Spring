<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>글 상세</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 40px;
            max-width: 700px;
            margin: auto;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .meta {
            font-size: 14px;
            color: #555;
            margin-bottom: 20px;
        }

        .content {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .button-group {
            display: flex;
            gap: 10px;
        }

        button {
            padding: 10px 16px;
            font-size: 14px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        button:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
<h1>{{title}}</h1>
<div class="meta">작성자: {{author}}</div>
<div class="content">{{content}}</div>

{{#isAuthor}}
    <div class="button-group">
        <form action="/articles/{{articleId}}/edit" method="get">
            <button type="submit">수정</button>
        </form>
        <form action="/articles/{{articleId}}/delete" method="post">
            <button type="submit" onclick="return confirm('정말 삭제하시겠습니까?')">삭제</button>
        </form>
    </div>
{{/isAuthor}}

<form action="/articles" method="get" style="margin-top: 20px;">
    <button type="submit">글 목록</button>
</form>
</body>
</html>
