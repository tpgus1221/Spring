<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>로그인</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding-top: 60px;
        }

        h1 {
            color: #2c3e50;
        }

        form {
            display: inline-block;
            text-align: left;
            margin-top: 30px;
        }

        label, input {
            display: block;
            margin-bottom: 15px;
            font-size: 16px;
        }

        input {
            width: 250px;
            padding: 8px;
        }

        button {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        button:hover {
            background-color: #2980b9;
        }

        .message {
            margin-top: 20px;
            color: red;
        }
    </style>
</head>
<body>
<h1>로그인</h1>

<form action="/login" method="post">
    <label for="id">ID:</label>
    <input type="text" id="id" name="id" required>

    <label for="pw">PW:</label>
    <input type="password" id="pw" name="pw" required>

    <button type="submit">로그인</button>
</form>

{{#errorMessage}}
    <p class="message">{{errorMessage}}</p>
{{/errorMessage}}
{{#result}}
    <p class="message">{{result}}</p>
{{/result}}
{{#logoutMessage}}
    <p class="message" style="color: green;">{{logoutMessage}}</p>
{{/logoutMessage}}

<form action="/signup" method="get" style="margin-top: 20px;">
    <button type="submit">회원가입</button>
</form>
</body>
</html>